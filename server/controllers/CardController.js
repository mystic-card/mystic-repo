const { Card } = require("../models/index");

class CardController {
  static async randomCards(req, res, next) {
    try {
    } catch (error) {
      console.log(error);
      next(error);
    }
  }
}
